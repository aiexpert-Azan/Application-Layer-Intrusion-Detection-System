# Exposes the gateway so it is reachable from outside the cluster.
apiVersion: v1
kind: Service
metadata:
  name: securids-service
spec:
  type: NodePort
  selector:
    app: securids-backend
  ports:
    - port: 8000
      targetPort: 8000
